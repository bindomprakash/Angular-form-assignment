import { EmpDirective } from './emp.directive';

describe('EmpDirective', () => {
  it('should create an instance', () => {
    const directive = new EmpDirective();
    expect(directive).toBeTruthy();
  });
});
